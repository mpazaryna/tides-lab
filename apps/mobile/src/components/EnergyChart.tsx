import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const EnergyChart = (props: Props) => {
  return (
    <View>
      <Text>EnergyChart</Text>
    </View>
  );
};

export default EnergyChart;

const styles = StyleSheet.create({});
